
var React = require('react')

var styles = require('../styles/styles.scss')

var App = React.createClass({
  render: function() {
    return (
      <div className="app">
        App
      </div>
    )
  }
});

module.exports = App
